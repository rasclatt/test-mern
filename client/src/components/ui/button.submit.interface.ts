export default interface IUiButtonSubmit {
    label: string,
    disabled?: boolean,
    loading?: boolean,
    loader?: boolean,
    minWidth?: number,
    className?: string,
    buttonClassName?: string,
}